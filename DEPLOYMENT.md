# دليل نشر الموقع على GitHub Pages

## الخطوات المطلوبة

### 1️⃣ إنشاء مستودع على GitHub

1. اذهب إلى [GitHub.com](https://github.com)
2. سجّل الدخول إلى حسابك (أو أنشئ حساب جديد)
3. انقر على زر **"New"** أو **"+"** لإنشاء مستودع جديد
4. اسم المستودع: `handcraft-textiles` (أو أي اسم تريده)
5. اجعل المستودع **Public** (عام)
6. **لا تضف** README أو .gitignore أو license (لأننا سنرفع الملفات الموجودة)
7. انقر على **"Create repository"**

### 2️⃣ ربط المشروع المحلي بـ GitHub

افتح Terminal/PowerShell في مجلد المشروع وقم بتنفيذ الأوامر التالية:

```powershell
# الانتقال إلى مجلد المشروع
cd C:\Users\asus\.gemini\antigravity\scratch\handcraft-textiles

# تهيئة Git (إذا لم يتم بعد)
git init

# إضافة جميع الملفات
git add .

# إنشاء أول commit
git commit -m "Initial commit: Arabic handcraft textiles website"

# ربط المستودع المحلي بـ GitHub (استبدل USERNAME باسم المستخدم الخاص بك)
git remote add origin https://github.com/USERNAME/handcraft-textiles.git

# رفع الملفات إلى GitHub
git branch -M main
git push -u origin main
```

### 3️⃣ تفعيل GitHub Pages

1. اذهب إلى صفحة المستودع على GitHub
2. انقر على **"Settings"** (الإعدادات)
3. من القائمة الجانبية، انقر على **"Pages"**
4. في قسم **"Source"**:
   - اختر Branch: **main**
   - اختر Folder: **/ (root)**
5. انقر على **"Save"**
6. انتظر 1-2 دقيقة حتى ينشر الموقع

### 4️⃣ الوصول إلى الموقع

بعد النشر، سيكون رابط موقعك:
```
https://USERNAME.github.io/handcraft-textiles/
```

استبدل `USERNAME` باسم المستخدم الخاص بك على GitHub.

---

## ملاحظات مهمة

- ✅ جميع الملفات جاهزة للنشر
- ✅ الموقع يعمل محلياً بشكل كامل
- ✅ لا حاجة لأي تعديلات إضافية
- ⚠️ تأكد من أن المستودع **Public** (عام) لكي يعمل GitHub Pages مجاناً
- 🔄 أي تحديثات مستقبلية: فقط قم بعمل `git add .` ثم `git commit -m "رسالة التحديث"` ثم `git push`

---

## حل المشاكل الشائعة

### المشكلة: الصور لا تظهر
**الحل**: تأكد من رفع مجلد `images` بالكامل

### المشكلة: الخطوط لا تظهر بشكل صحيح
**الحل**: الخطوط محمّلة من Google Fonts وتعمل تلقائياً عبر الإنترنت

### المشكلة: الموقع لا يظهر بعد النشر
**الحل**: انتظر 2-3 دقائق إضافية، ثم حاول تحديث الصفحة (Ctrl + F5)

---

## هل تحتاج مساعدة؟

إذا واجهت أي مشكلة في النشر، أخبرني وسأساعدك! 🚀
